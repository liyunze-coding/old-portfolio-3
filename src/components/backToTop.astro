---
---

<div
	id="backToTop"
	class="fixed z-10 bottom-5 right-5 text-2xl text-center bg-gray-500 w-10 h-10 rounded-full flex justify-center items-center cursor-pointer transition-transform duration-100 translate-y-28"
>
	⇧
</div>

<script is:inline>
	const backToTop = document.getElementById("backToTop");
	backToTop.addEventListener("click", () => {
		window.scrollTo({
			top: 0,
			behavior: "smooth",
		});
	});

	window.addEventListener("scroll", () => {
		if (window.scrollY > 50) {
			backToTop.style.transform = "translateY(0)";
		} else {
			backToTop.style.transform = "translateY(100px)";
		}
	});
</script>
